technologies:
- twilio - for sms
- auth0 or nextAuth?

pages:
- /checkin (non user)
- /signup 
- /signin (returning user)
- /waitlist (allows everyone to see the checkin line)
- /pages/admin/
	-/dashboard
	-/manage


todo:
- create page where user can see their points after signin / checkin / signup
- checking -> enter phone number -> send confirm to user, and add user to waitlist, redirect to waitlist