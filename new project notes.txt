technologies:
- twilio - for sms
- auth0 or nextAuth?

pages:
- /checkin (non user)
- /signup 
- /signin (returning user)
- /waitlist (allows everyone to see the checkin line)
- /admin/
	-/dashboard
	-/manage


todo:
- check in -> enter phone number -> send confirm to user (twilio), and add user to waitlist, redirect to waitlist
- unify theme colors
- add spinners during api calls -> and disable button
- fix issue with navbar waitlist link getting cut off